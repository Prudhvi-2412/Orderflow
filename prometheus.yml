global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'orderflow-api-gateway'
    metrics_path: '/api/metrics'
    static_configs:
      - targets: ['host.docker.internal:4000', 'localhost:4000']
